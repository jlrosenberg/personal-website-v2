import { createMuiTheme } from '@material-ui/core'

const theme = createMuiTheme({
  palette: {
    primary: {
      main: `#e04343`,
    },
    secondary: {
      main: `#FFE800`,
    },

    text: {
      primary: `#353535`,
      secondary: `#a1a9b0`,
    },
  },
})

export default theme
